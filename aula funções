// this is a js file
// Franciele Dörr do Santos

function minhaFuncao(variavel) {
    return variavel * 5
    }
    minhaFuncao(2)
    console.log(minhaFuncao(2))
    minhaFuncao(10)
    console.log(minhaFuncao(10))

    //1.
    //a) varor das variaveis. 10, 50

    //b) Não terá impressão.

    //2.
    
//a) caixa de texto; para inserir um texto. Vai converter o texto em letras minusculas e dizer se contem a palavra "cenoura" true ou false.
//b)true.


//a)
function retornaTexto(){
    console.log("Eu sou Caio, tenho 23 anos, moro em São Paulo e sou estudante.");
} 
retornaTexto();


//b)
function retornaTextoComParametros (nome, idade, cidade, profissao){

return `Eu sou ${nome}, tenho ${idade} anos, moro em ${cidade} e sou ${profissao}.`;}


console.log(retornaTextoComParametros("Franciele", 28, "São Leopoldo", "Estudante"));

//2.a)
let num1 = 3
let num2 = 4

function calcularSoma(num1, num2) {
    const Soma = num1 + num2
    console.log(Soma)
}
const areaCalculada = calcularSoma(3 + 4) // 7

//b.

function retornaMaior(num1, num2) {
    const Soma = num1>= num2

    console.log(Soma)
}
//c.
function retornaPar(num1, num2) {
    const Soma = num1%2 === 0;
    console.log(Soma)

    //d...
    
